# ── Core dependencies ─────────────────────────────────────────────────────────
redis>=5.0.0
kubernetes>=29.0.0
# pydantic v2 requires pydantic-core (Rust). Pin to latest stable wheel build.
# If your environment cannot build wheels, install via:
#   pip install pydantic --prefer-binary
pydantic>=2.0.0

# ── Runtime utilities ─────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # optional: load .env in local dev

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
