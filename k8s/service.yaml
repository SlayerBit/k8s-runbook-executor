apiVersion: v1
kind: Service
metadata:
  name: agent2
  namespace: food-app
  labels:
    app: agent2
spec:
  type: ClusterIP
  selector:
    app: agent2
  ports:
    - name: http
      port: 80
      targetPort: health
      protocol: TCP

