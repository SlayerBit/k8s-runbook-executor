apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent2
  namespace: food-app
  labels:
    app: agent2
    app.kubernetes.io/managed-by: agent2
  annotations:
    # If using Workload Identity (GKE) or IRSA (EKS), annotate here:
    # iam.gke.io/gcp-service-account: agent2@<project>.iam.gserviceaccount.com
automountServiceAccountToken: true
